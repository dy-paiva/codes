web:
  build: .
  command: /bin/sh -c "rm -f /pessoaApp/tmp/pids/server.pid && rails s -p 3000 -b '0.0.0.0'"
  volumes:
    - .:/pessoaApp
  ports:
    - "8000:3000"
  links:
    - db
db:
  image: postgres
